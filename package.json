{
  "name": "quiz-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Quiz Builder — Express/Prisma API and Next.js client",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c green,blue \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
